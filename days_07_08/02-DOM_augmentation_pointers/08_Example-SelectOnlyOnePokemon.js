// set up every image on the page to respond to click events
// (get a reference to all of the images using their class name)

    // if the image is clicked, add the blue class to it


    // what happens when you click a second image?
    // we only want one selected image at a time

